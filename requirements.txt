# 動作確認済みのバージョンに固定。
# サーバー(MWS)は再デプロイのたびにここを見てインストールし直すため、範囲指定だと
# ライブラリ側の更新でコードを変えていないのに突然壊れることがある。
# 更新するときは手元で `pip install -U <パッケージ>` → テストを通してから書き換える。
discord.py==2.7.1
aiohttp==3.13.3
aiosqlite==0.22.1
python-dotenv==1.2.2
